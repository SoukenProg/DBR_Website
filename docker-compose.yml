services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4004:3000"
    env_file:
      - ./.env.production
    environment:
      NODE_ENV: production
    restart: unless-stopped
